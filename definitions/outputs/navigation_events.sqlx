config {
    type: "incremental",
    tags: [dataform.projectConfig.vars.environment],
    database: dataform.projectConfig.vars.source_database,
    schema: dataform.projectConfig.vars.target_aggregated_dataset,
    bigquery: {
        partitionBy: "event_date",
        requirePartitionFilter : true,
        updatePartitionFilter: "event_date >= min_event_date"
    },
    uniqueKey: [
        "event_date",
        "event_name",
        "cleaned_page_location",
        "canonical_url",
        "link_domain",
        "link_url",
        "type",
        "outbound"
    ],
    dependOnDependencyAssertions: true
}

pre_operations {
  DECLARE min_event_date DATE;
  SET min_event_date = (
    SELECT MIN(event_date)
    FROM  ${ref("staging_navigation_events")}
    );
}

SELECT *
FROM ${ref("staging_navigation_events")}
