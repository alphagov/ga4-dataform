config {
    type: "incremental",
    tags: [dataform.projectConfig.vars.environment, "aggregate-analytics-selector"],
    database: dataform.projectConfig.vars.source_database,
    schema: dataform.projectConfig.vars.target_flattened_dataset,
    bigquery: {
        partitionBy: "event_date",
        requirePartitionFilter : true,
        clusterBy: ["event_name"]
    }  
}
 
SELECT 
  *
FROM ${ref("ga4_augmenting")}